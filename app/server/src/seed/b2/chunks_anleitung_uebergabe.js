/**
 * No vocabulary experience — this source exists to fill the Goethe Lesen
 * Teil 5 format gap (written instructions) rather than to teach new
 * expressions.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_anleitung_uebergabe" };
