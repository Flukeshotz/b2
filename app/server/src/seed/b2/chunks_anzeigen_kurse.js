/**
 * No vocabulary experience for this source — it exists to fill a FORMAT gap
 * (Goethe Lesen Teil 3, ad-to-situation matching) rather than to teach new
 * expressions.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_anzeigen_kurse" };
