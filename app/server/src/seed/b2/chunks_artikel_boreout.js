/**
 * No vocabulary experience — this source exists to fill the Goethe Lesen
 * Teil 4 format gap (longer single-author argumentative text) rather than
 * to teach new expressions.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_artikel_boreout" };
