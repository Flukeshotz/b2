/**
 * No vocabulary experience — this source exists to fill the telc Lesen
 * Teil 3 format gap (Richtig/Falsch/Text sagt dazu nichts) rather than to
 * teach new expressions.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_artikel_telearbeit" };
