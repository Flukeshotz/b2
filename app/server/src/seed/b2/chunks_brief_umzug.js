/**
 * No vocabulary experience for this source — it exists to fill a FORMAT gap
 * (Goethe Lesen Teil 1, personal correspondence) rather than to teach new
 * expressions. Empty on purpose, not an oversight.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_brief_umzug" };
