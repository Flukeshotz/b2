/**
 * No vocabulary experience — this source exists to fill the telc
 * Sprachbausteine format gap, not to teach new expressions.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_sprachbausteine_praxis" };
