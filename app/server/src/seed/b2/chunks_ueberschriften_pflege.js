/**
 * No vocabulary experience — this source exists to fill the telc Lesen
 * Teil 1 format gap (headline-to-paragraph matching) rather than to teach
 * new expressions.
 */

const EXPRESSIONS = [];
const BY_ID = new Map();

module.exports = { EXPRESSIONS, BY_ID, SOURCE_ID: "src_ueberschriften_pflege" };
